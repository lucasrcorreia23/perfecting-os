@AGENTS.md

# Perfecting v1

Gestão de clientes com workflow de Customer Success. UI **100% pt-BR**.

## Stack

- Next.js 16 (App Router, Turbopack) + React 19 + TypeScript strict.
- Tailwind CSS 4 (CSS-first: tokens em `app/globals.css` via `@theme inline`; sem tailwind.config).
- Supabase (Postgres + Auth + Storage) via `@supabase/ssr`. Migrations em `supabase/migrations/`, seed em `supabase/seed.sql`.
- Ícones: somente Heroicons 2 (`@heroicons/react/24/outline` padrão).
- Kanban: `@dnd-kit/core` (isolado em `src/components/workflow/board.tsx`).
- Testes: **Vitest** (`vitest run`). Specs `*.test.ts(x)` co-localizadas em `src/`; alias `@/*` resolvido em `vitest.config.ts`.

## Testes antes do push (obrigatório)

- **Sempre rode `npm test` e garanta verde antes de qualquer `git push`.**
- Enforcement: hook `.githooks/pre-push` (versionado; `git config core.hooksPath .githooks` é aplicado pelo script `prepare` no `npm install`) roda `npm test` e cancela o push se falhar. Pular só em emergência com `git push --no-verify`.
- Ao mexer em lógica pura de `src/lib/*` (format, kpis, utils…), adicione/atualize as specs correspondentes.

## Design

**Leia `docs/design-guidelines.md` antes de escrever qualquer componente** — ele vence qualquer hábito. Regras inegociáveis:

- Empilhamento vertical sempre `flex flex-col gap-*` — **nunca** `space-y-*`.
- Superfície padrão `bg-white rounded-sm border border-slate-200`; estilo orgânico: raios 16/20/28 (tokens sobrescritos) em containers e **controles pill** (`rounded-full`: botões, inputs, selects, busca, itens de menu) — **nunca** `rounded` sem sufixo. Sem blur/glassmorphism.
- Primária `#2E63CD` com parcimônia; cinzas só Slate; foco visível em tudo; tap targets ≥ 44px no mobile; `min-h-[100dvh]`.
- Placeholders usam chip "Em breve" (`#FFFBEB` / `#973C00`).

## Estrutura

```
app/(auth)/…            login, cadastro, recuperar
app/(app)/…             páginas autenticadas (shell com header + sidebar)
app/(calculadora)/…     calculadora de ROI pública por token (ver "Calculadora de ROI")
app/api/publico/…       API REST sem sessão (site externo + autosave da calculadora)
src/lib/                supabase clients, auth gate, actions ('use server'), constants, format, kpis
src/lib/api/            CORS, tokens, envelope de resposta e queries da API pública
src/lib/calculadora/    motor puro da calculadora ROI (V5) + estado/preço/trajetória
src/components/ui/      primitivos do guideline (Button, Tabs, ActionMenu, modais…)
src/components/…        componentes por feature (clients, activities, files, workflow, dashboard, profile, marketing, calculadora, shell)
proxy.ts                refresh de sessão + proteção de rotas
```

- Alias `@/*` → `./src/*`. Server Components por padrão; `"use client"` só onde há interatividade. Mutações via Server Actions em `src/lib/actions/*` (sempre revalidam sessão/role; RLS é o backstop).
- Roles: `interno` (acesso total) e `cliente` (só o próprio `/clientes/[id]` e `/perfil`) — gate server-side em `src/lib/auth.ts` (`getSessionProfile`/`requireInterno`), não só no proxy.

## Next 16 — diferenças que quebram build

- `middleware.ts` virou **`proxy.ts`** (função `proxy`; runtime Node; não exportar `runtime`).
- `cookies()`/`headers()`/`params`/`searchParams` são **async** — sempre `await`.
- `revalidateTag` exige 2º argumento — usamos só `revalidatePath`.
- Em dúvida, consulte `node_modules/next/dist/docs/`.

## Módulo Marketing

CMS de blog + funis de qualificação, 100% interno (`requireInterno` no layout). Três seções: `/marketing/blog`, `/marketing/funis`, `/marketing/leads`.

**Passo a passo de setup e integração com o site: `docs/marketing-integracao.md`.**

Duas telas fogem do shell/listagem padrão:

- `/marketing/funis/[id]/testar` — quiz em tela cheia (uma pergunta por vez) que simula o visitante. Vive em `app/(quiz)/`, um route group **sem `AppShell`** e com `requireInterno` próprio. Sem `?versao`, testa o rascunho salvo; com `?versao=publicada`, o snapshot que o site serve. Usa o mesmo `validateAnswers`/`scoreSubmission` da API pública e **não grava nada**.
- `/marketing/leads/[id]` — detalhe do lead (era modal). Respostas em `whitespace-pre-wrap`, pontuação, contato, UTM, status e notas. Exporta CSV pelo `leadsToCsv` de `src/lib/marketing-lead-export.ts`; a listagem exporta o **recorte filtrado** pela mesma função.

**API pública** (`app/api/publico/*`) — o site externo nunca fala com o PostgREST, só com estes handlers:

| Método | Rota | Auth |
|---|---|---|
| GET | `/api/publico/posts?limit&offset&tag` | `Authorization: Bearer <MARKETING_API_TOKEN>` |
| GET | `/api/publico/posts/[slug]` | idem |
| GET | `/api/publico/funis/[slug]` | idem |
| POST | `/api/publico/funis/[slug]/respostas` | idem **ou** `X-Perfecting-Token: <MARKETING_PUBLIC_TOKEN>` + `Origin` na allowlist |

- Envelope: `{ data, meta? }` / `{ error: { code, message, field? } }`. `Cache-Control: no-store` — **quem cacheia é o site** (`next: { revalidate: 60 }`, obrigatório: sem cron, é a revalidação periódica que faz o post agendado aparecer).
- `OPTIONS` exportado em cada `route.ts` (o automático do Next não manda headers de CORS) e `Vary: Origin` sempre.
- `src/lib/supabase/proxy.ts` faz **bypass de `/api/publico` antes de tudo** — sem isso o preflight vira `307 → /login` e o browser reporta falha de CORS opaca.
- Envs: `SUPABASE_SERVICE_ROLE_KEY`, `MARKETING_API_TOKEN`, `MARKETING_PUBLIC_TOKEN`, `MARKETING_ALLOWED_ORIGINS`, `MARKETING_IP_SALT`, `NEXT_PUBLIC_MARKETING_SITE_URL`.
- O site externo **precisa sanitizar** o `body_md` que recebe (`rehype-sanitize`/DOMPurify). Do nosso lado, `validatePostInput` já rejeita `<script>`, `<iframe>`, `javascript:` e `on*=` no save.

## Calculadora de ROI ("Encaminhar calculadora")

Fluxo comercial **self-service**: o interno gera um link público tokenizado definindo **só a validade** (+ identificação interna opcional); a pessoa acessa `/calculadora/[token]` **sem login** e atravessa quatro etapas — mensalidade, quiz de 8 perguntas, relatório, exportar —, **montando a própria proposta** (plano, assentos, prazo e cenário, na pergunta 8) e enviando no fim — é a proposta comercial que ela mesma montou. A aba **Calculadora** do cliente (só interno; `requireInterno` explícito também no detalhe) mostra links, progresso, a proposta montada, o resultado e a timeline.

Dois pontos de entrada: **"Encaminhar calculadora"** no perfil/listagem de clientes (link já atado ao cliente) e **"Gerar calculadora"** ao lado de "Novo cliente" em `/clientes` (link **avulso**, `client_id null`), listado na seção "Calculadoras avulsas" e importável depois para qualquer perfil (`linkCalculatorToClient`, evento `vinculado_a_cliente`; a mesma ação move um link entre clientes). Detalhe da avulsa antes de vincular: `/calculadoras/[linkId]` (redireciona ao detalhe do cliente se já vinculada).

- **Fonte da verdade do racional matemático é híbrida** (decidido em 17/08/2026; fonte atualizada em 18/08/2026): `docs/referencia/Perfecting_ROI_Calculator_Template.xlsx` (SHA `96c88e20…`, 7 abas em pt-BR: Leia-me / Tabela de Preços por Tier / Premissas / Equipes / Motor / Conta / Comparação de Cenários) **manda onde os dois falam**, junto de `docs/referencia/Referencia-Completa-Formulas-ROI-Perfecting.pdf` (SHA `42d8f5f7…`), que documenta a planilha célula a célula. O Template SUBSTITUI o `Perfecting_ROI_Calculator_FIESC.xlsx` (SHA `9f2fb833…`) como fonte — a auditoria de 18/08 confirmou que **o racional não mudou**: todas as constantes da aba Premissas são idênticas às de `constants.ts` (encargos 1,75; jornada 200; supervisão 0,25; haircut 0,7 em rampa/ciclo/conversão e nenhum em ticket; evento 0,5; escopo 2,1 na faixa [0,25;6]; piso 13.000; 22/264 dias; checagem 25%; tetos 0,30/0,80/0,30 e conversão dinâmica; os três presets de cenário; desconto de prazo 0). O que o Template traz é tradução, a aba comercial nova e o PDF. `CALCULADORA_ROI_RACIONAL_CONSOLIDADO_V5.md` continua valendo onde os dois são omissos; — trajetória editável (§4.12), níveis de serviço, linhas não somadas extras, estrutura compartilhada (§4.11). **Dois goldens coexistem** em `calc.test.ts`/`modelo.test.ts`: §14 (30 vendedores, Prática, ROI 1,951×, payback 6,15) e FIESC (100 vendedores, Intensivo, funil preenchido, ROI 0,432×, payback 27,77, mensal 68.064 sem piso). Divergência código↔fontes é incidente — não "corrigir" nenhum dos lados silenciosamente.
- **O que o Excel corrigiu no código** (17/08/2026): tetos de fine-tune viraram literais (ticket 0,30 e rampa 0,80, acima do preset otimista); Δciclo bifurcou (ciclo ≥ 7 dias ⇒ dias inteiros com piso de 1 e teto `round(ciclo×0,3)` half-up; abaixo de 7 ⇒ percentual contínuo do cenário, `deltasEfetivos` devolve `cicloPct`); margem virou % livre; headcount de referência passou a seguir Engine!C88–C91; nasceu o aviso `payback_excede_contrato` (Engine!C83/Account!C32). **Divergências deliberadas mantidas**: o gating aceita gestor/prática em 0 (o Excel exige > 0 — superset consciente, time sem programa de treino existe), e `compararCenarios` recalcula o ganho de ciclo em cada cenário, corrigindo o quirk `=C19` da planilha (que herdava o ciclo do cenário ativo nas colunas Realista/Otimista). Teste anti-quirk em `cenarios-comparacao.test.ts`.
- **Segunda rodada de alinhamento ao Excel (18/08/2026)**, da auditoria célula por célula do mesmo arquivo (`Perfecting_ROI_Calculator_FIESC.xlsx`, SHA idêntico ao de 17/08 — o download "(1)" é o mesmo arquivo). Três lacunas fechadas: **(a) assentos travam no tamanho do time** (`Engine!C19 = MIN(assentos, vendedores)`, em `assentosEfetivos`) — a cobertura já satura em 1, então assento acima do time não gera retorno e cobrá-lo derrubava o ROI por uma escolha que não muda o valor (150 assentos para 100 vendedores davam 1.200 h / R$ 95.068 / ROI 0,309, contra 800 h / R$ 67.068 / ROI 0,438 do Excel). O estado guarda o que a pessoa digitou; o teto é derivado, então subir o time devolve os assentos. `TimeModelo.assentosLimitados` diz à tela por que o número parou. **(b) Só time completo entrega horas à conta** (`Account!C13 = SUM(Engine!C20:L20)`, filtro em `computarModelo`): um irmão preenchido pela metade barateava a taxa combinada e mexia no ROI do time que já tinha fechado (0,4534 em vez de 0,4385). Enquanto **nenhum** time fechou, a base cai para todos os assentos — ali o preço é prévia da proposta em construção, nunca denominador de um ROI, e "Quanto custa" avisa quem ficou de fora. **(c) `tetoFunil` (`Engine!C64–C69`) virou saída do motor**: a trava do funil era invisível quando cortava a parcela sem zerá-la; agora a explicação do ciclo mostra capacidade liberada × oportunidades ociosas. **Continuam divergindo de propósito**: o gating aceita 0 nos campos de estrutura (superset, acima), `compararCenarios` corrige o quirk `=C19`, e exigimos o custo do evento quando o caminho é "evento" (o Excel só exige no "externo" — lá o branco vira zero por `N()` e apaga a eficiência inteira sem avisar; aqui o zero é declarado). **Fora do código de propósito**: `Engine!C85` (valor por vendedor/ano) é a régua diária noutra unidade, e a série linear de `Account!B56–E70` é "presentation only" — o painel B cruza o custo anual exatamente no payback (invariante 7), enquanto a coluna "Paid off?" da planilha só responde "sim" se o ROI anual ≥ 1.
- **Terceira rodada — Template de 18/08/2026** (`docs/referencia/`). O racional não mudou; três coisas mudaram em volta dele. **(a) Tier 2 da escada foi de 573 para 656 h.** A aba comercial "Tabela de Preços por Tier" e a aba Premissas do MESMO arquivo se contradizem (656 contra `C31 = 573`, e é Premissas que o staircase da aba Conta lê). Decisão do decisor em 18/08: vale a tabela comercial, que é a que vai ao cliente. Nenhuma conta abaixo de 574 h/mês muda; de 656 em diante a diferença trava em **+R$ 996/mês** (83 horas voltam de R$ 70 para R$ 82). O golden §14 (120 h, piso) não mexeu; o FIESC foi inteiro: 67.068 → **68.064**, taxa 83,835 → **85,08**, ROI 0,4384788 → **0,4320624**, payback 27,3673 → **27,7738**. **A planilha foi corrigida em 19/08/2026** (E-24 fechada): `Premissas!C31` é 656, os rótulos das faixas acompanham em Premissas e Conta, e a escada da aba Conta volta a reproduzir os dois goldens ao centavo — o FIESC volta a ser "o que o Excel devolve". O SHA do Template mudou por isso. **(b) Planos renomeados para Leve (2 h) / Padrão (4 h) / Intensivo (8 h)**, nomes da aba comercial. Só os LABELS: as chaves `essencial`/`pratica`/`intensivo` são persistidas em `proposta.plano` e trocá-las descartaria a proposta de todo link salvo, pelo mesmo motivo que `v: 2` não é bumpado. O rename desfaz de quebra a colisão em que "Essencial" nomeava um plano E um nível de serviço. **(c) Nasceu `/calculadoras/referencia`** (abaixo). **Incoerências do Template, a corrigir do lado da planilha** (os 656 contra 573 foram corrigidos em 19/08; seguem abertas): os rótulos da coluna B do Motor dessincronizados das fórmulas (B71 diz "Valor anual total" onde o PDF documenta C72; B39 e B41 repetem "Eficiência (R$/ano)" para a linha bruta e a limitada pelo teto; sobrou inglês em B21/B40/B42/B75/B78/B83/B87); e o 2 h/assento aparecendo como "Essencial" em Premissas e "Leve" na aba comercial.
- **O PDF das fórmulas é servido pelo token do link, e só depois que existe resultado** (`/api/publico/calculadora/[token]/formulas` + botão "Fórmulas (PDF)" no header público). Não fica em `public/`: ali a URL é adivinhável e o documento estaria aberto para a internet inteira. Pela rota ele alcança exatamente quem já podia ver os números, e para de alcançar quando o link expira ou é revogado — mesma `linkAceitaAcesso` da página. O arquivo mora em `docs/referencia/` (uma cópia só, junto da planilha que ele descreve), e `outputFileTracingIncludes` no `next.config.ts` é o que o faz subir no bundle — nada no código o importa, então o tracing não o acharia sozinho. **A trava é dupla e de propósito:** o botão fica VISÍVEL e bloqueado enquanto nenhum time está completo (escondido, ninguém descobre que a referência existe; bloqueado, o balão diz o que falta), e a ROTA repete a condição — botão desabilitado é afordância, não controle de acesso, e quem monta a URL na mão passaria por cima dele. O bloqueado é `<button aria-disabled>`, não `<a>` sem href nem `disabled` de verdade: sem href não há endereço a copiar, e `aria-disabled` mantém o controle focável, que é o que permite descobrir pelo teclado por que ele não responde. A condição ("existe ao menos um time completo") está pinada em `validacao-passo.test.ts`, porque as duas pontas a leem.
- **A referência de fórmulas é DERIVADA do código, não transcrita do PDF** (`src/lib/calculadora/referencia.ts` + `referencia-formulas.tsx`, em `/calculadoras/referencia`, `requireInterno` explícito). Dado puro no padrão do `faq.ts`: cada entrada tem célula da planilha, fórmula em notação de leitura, explicação, o `arquivo.ts#símbolo` onde a conta vive e — onde existe — a divergência declarada. **Todo número sai de `constants.ts`**, e `referencia.test.ts` falha se algum se soltar da constante; transcrever o PDF criaria um segundo lugar onde os números moram, e ele começaria a mentir no primeiro ajuste do motor. É o mapa célula↔função que antes só existia espalhado em comentários. As divergências deliberadas moram nela — duas do motor: o gating aceita 0 em gestor/prática (o Excel exige > 0) e exige custo do evento no caminho "evento" (o Excel só exige no "externo"); e `compararCenarios` recalcula o ciclo nos três cenários (o Template ainda herda o ativo no Conservador, `=SUM(Motor!C68:L68)`). Eram três até 19/08/2026: a fronteira do Tier 2 saiu da lista quando a planilha foi corrigida para 656, e um teste falha se alguém reabrir a contradição. Mais cinco na seção `coi` (abaixo). O teste de integridade valida **arquivo e símbolo**: validar só o arquivo deixou `estrutura.ts#ratearEstrutura` (o certo é `aplicarEstrutura`) passar por duas auditorias.
- **Custo da Inação (COI): a leitura contrafactual, e ela NUNCA se soma ao ROI** (`coi.ts` + `custo-inacao.tsx`, bloco "O que está em jogo hoje" entre "De onde vem o número" e "Quanto custa"). Vem da aba "Custo da Inação" de `ROI_Perfecting_Corrigido.xlsx` (19/08/2026), que é **insumo e não fonte** — o Template e o PDF seguem mandando no motor, e o arquivo NÃO foi copiado para `docs/referencia/`; o registro é a auditoria E-30…E-36. **O invariante 1 do V5 é contrafactual ⊻ atribuição**, e é ele que decide a arquitetura: o COI mede o que vaza sem o programa, o ROI mede o que o programa devolve, e os dois medem os mesmos mecanismos (rampa, conversão, ciclo) por lados opostos. A tela mostra `recuperado = min(valorAno, total)` e `residual = max(0, total − valorAno)` — em lugar nenhum existe `COI + valorAno`. A nota metodológica ① da aba pede exatamente essa soma (E-34); somados, os goldens dariam um "custo de não agir" de 5,88× (§14) e 2,62× (FIESC) o valor que a linha de cima promete, e a tela desmentiria a si mesma. `calcCoi` é chamado em `computarModelo` DEPOIS de `calcResultadoTime` e o resultado vive em `TimeModelo.coi` (`null` se incompleto); um teste no molde do invariante 4 garante que ler o COI não mexe em `roi`/`payback`/`valorAno`. **Cinco correções declaradas**, todas em `referencia.ts` seção `coi`: (a) as dimensões 2/3/5 saem em **margem**, não receita (E-33); (b) a cobertura é medida **em horas, uma vez só** — a aba mede a Dimensão 1 em cabeças e o Diagnóstico em horas, e as duas se contradizem nos dois goldens (E-31), enquanto o bloco `capacidade` sobrevive dizendo **outra coisa** (se o gestor sequer tem as horas: no §14 tem, e o problema é escopo; no FIESC não tem); (c) o primeiro ramo de `C9` é descartado por multiplicar horas por gestores onde o rótulo pede vendedores, encobrindo o segundo ramo, que é o certo (E-32); (d) haircut nas dimensões 4 e 6, que a nota ② promete e a aba esquece (E-35) — a 5 fica de fora porque ali quem faz esse papel é `COI_FRACAO_COACHAVEL`; (e) o turnover incide só sobre os não atendidos e trava em `contratacoesAno`, porque ninguém perde mais gente do que repõe. Cada dimensão é clampada em zero (cobertura acima do mínimo não vira crédito) e as duas que dependem de `salarioVendedor` — campo opcional — devolvem `null`, travessão com atalho ao passo 3, nunca zero. O bloco é **todo slate**: verde é "entra na conta", e o COI por definição não entra. Não imprime; o que vai ao papel é uma frase dentro de `#resumo-verificavel`, e só no time único. **As constantes são `[H]`** e as fontes citadas na aba (MySalesCoach 2026, Ebsta, Gartner, Dixon/McKenna, Deloitte, CareerTrainer.ai) aparecem **só** em `/calculadoras/referencia` — nada de nome de pesquisa na tela do visitante até ratificação, pelo mesmo motivo que o `Perfecting-CFO-QA-Guide.docx` foi recusado.
- **A proposta vive no `state`, não numa config do interno.** `EstadoCalculadora` (`{ v: 2, prazoMeses, estrutura?, times: [...] }`) é dono dos times (id, nome, proposta, entradas, cenário, trajetória). `assentosEfetivos()` resolve o default **e o teto**: assentos vazios = **os vendedores do time** (cobertura 1, como no §14) e assentos escolhidos travam nesse mesmo número (`Engine!C19`, ver alinhamento de 18/08 abaixo). Sem `numVendedores` não há número — P6 preservado.
- **Gating por tipo**: `ResultadoTime` é união discriminada; incompleto ⇒ não existem números, o formatador devolve travessão. Nunca resultado parcial, nunca NaN/zero indevido.
- **Token derivado por HMAC** (`src/lib/api/calculator-token.ts`): `HMAC(CALCULATOR_LINK_SECRET, "calc-link:{id}:{versão}")`; o banco guarda só o sha256 do token. "Copiar link" rederiva a URL a qualquer momento; rotação = versão+1. Env nova: `CALCULATOR_LINK_SECRET`.
- **`src/lib/api/calculator-queries.ts` é o 2º consumidor autorizado do service role** (o gate do visitante é o token, não sessão). O proxy faz bypass de `/calculadora` antes do `getUser()`; o autosave (`POST /api/publico/calculadora/[token]/estado`) é same-origin — sem CORS/OPTIONS. Nomes de time são a **única string livre** do visitante: sanitizados em `sanitizarNomeTime` (controle + clamp 60).
- **Status do link é derivado** (`linkStatus`): revogado > expirado > concluído > ativo. Pós-envio o link continua editável até expirar; edições geram evento `editado_pos_envio` coalescido (10 min). Acesso é sessão deduplicada (30 min por link+ip_hash). O evento `enviado` carrega a proposta montada (plano/assentos/prazo/preço).
- **Cenários + sliders**: presets do §4.8 (Conservador default) + modo "parâmetros personalizados"; `deltasEfetivos()` é o único caminho de deltas para o cálculo e clampa tudo pelos tetos do modelo — sliders nunca relaxam travas. `FINE_TUNE_TICKET_MAX = 0,30` e `FINE_TUNE_RAMPA_MAX = 0,80` são **literais da aba Assumptions do Excel** e ficam ACIMA do preset otimista: o slider pode passar do cenário, nunca do modelo (invariante 15 preservado — o teto tem fonte documental, só mudou a fonte; antes eram derivados de `CENARIOS.otimista`). Conversão mantém o teto dinâmico `min(5, c×0,4, 100−c)`; ciclo mantém 30%.
- **Comparação de cenários** (`cenarios-comparacao.ts`, aba Scenario Comparison): os três presets recalculados com as MESMAS entradas, para a decisão se tomar sobre a faixa e não sobre um ponto. A eficiência é invariante entre eles (vem do caminho declarado, não do otimismo). Aparece como bloco visual no fim de "De onde vem o número" e alimenta o FAQ.
- **Consolidado multi-time é sempre ponderado** (Σvalor/Σpreço, invariante 11) — nunca média de ROIs. Editar a trajetória **não** altera ROI/payback (invariante 4: `calcResultadoTime` nem recebe a trajetória).
- **Estrutura de capacitação compartilhada** (§4.11): quando os mesmos gestores atendem vários times, declarar tudo por time contaria a mesma economia N vezes. `estrutura.ts` faz o rateio — **gestores e custos do contrafactual por peso de vendedores; horas por gestor, vendedores por gestor e salário passam inalterados** — como transformação pura ANTES do cálculo, em `computarModelo`. Assim `calc.ts` e `consolidado.ts` não mudam. O fator de escopo é invariante ao rateio (o nº de gestores cancela na fração). É campo **opcional** do estado, mantendo `v: 2`: bumpar a versão faria `parseEstado` descartar o preenchimento de todo link já salvo. Formulários editam o estado **cru** (`estadoTime.entradas`); resultado e gating leem o **derivado**.
- **Nível de serviço**: por assentos (30/100), com **um degrau a mais em contratos de 24 meses** (§4.9). Sem efeito sobre preço — `precoConta` devolve `nivelPorPrazo` para a tela declarar de onde veio o degrau.
- **Sem custo interno no código** (§9 do V5): `constants.ts` da calculadora só tem preço de venda (escada 98/82/70/60, piso R$ 13.000) e premissas declaradas. `PCT_EVENTO_SUBSTITUIVEL = 0,5` é hipótese [H] decidida em 15/08/2026, pendente de ratificação do decisor.
- **Margem é % livre** (`margemPct`, 0–100), como no Excel — as 6 faixas de 15/08/2026 foram superadas em 17/08. O atalho "Não sei" preenche 30 (`MARGEM_NAO_SEI`) em vez de ser um enum à parte. **Retrocompat obrigatória**: `sanitizarEntradas` lê `margemFaixa` de estados antigos pelo valor central (`MARGEM_LEGADO`), e `margemPct` explícita vence o legado — sem isso, todo link já preenchido "des-preencheria" ao reabrir (progresso cai, resultado some). O estado segue `v: 2`: bumpar a versão descartaria o preenchimento de todo link salvo.
- Persistência server-side (autosave + eventos) **substitui, só neste fluxo**, a decisão "sem backend" do §8 do V5 (que valia para a ferramenta standalone). LGPD: sem login/e-mail do visitante; IP só como HMAC.
- **O quiz de 8 perguntas FOI adotado em 20/08/2026** — a decisão de 18/08 foi revertida pelo decisor. O `perfectingroiuxflow.md` descrevia uma pergunta por tela, no máximo dois campos numéricos cada, e em 18/08 recusamos a arquitetura por avaliarmos que ela custaria o wizard de 5 passos, o multi-time, a estrutura compartilhada e a trajetória; o handoff de 19/08 repetiu a recusa. **O que mudou é o diagnóstico, não a régua**: o passo 1 pedia cinco números de uma vez, e o leitor-alvo da §8.12b (finanças, 45+, abre o link uma vez) desistia antes de responder o primeiro. `PASSOS` passou a 8 entradas e `PassoId` (`types.ts`) é `1 | 2 | 3 | 4 | 5 | 6 | 7 | 8` — **nenhum componente declara mais a união literal**, senão adicionar uma pergunta quebraria seis arquivos em silêncio. Dois detalhes do desenho: a pergunta 7 é a **opcional** (funil), e `passoCompleto` deriva isso de `passo.opcional` em vez do número 5 cravado, com a regra continuando em `funilPreenchido`; a pergunta 8 tem **`campos: []`** porque edita a PROPOSTA (plano, assentos, prazo, cenário), que vive fora de `EntradasTime` — array vazio faz `every` devolver true, que é o certo, já que ali não há o que faltar. `PASSO_OPCIONAL` e `ULTIMO_PASSO` são derivados de `PASSOS`, nunca literais espalhados. **O que NÃO mudou, e era o custo temido**: multi-time, estrutura compartilhada (§4.11), trajetória e o motor seguem intactos — o gating continua em `calc.ts` e `validacao-passo.ts` continua recortando `camposFaltando` pelos `PASSOS[].campos`, sem reimplementar obrigatoriedade.
- **A jornada tem QUATRO etapas nomeadas, e é por elas que se volta** (20/08/2026, mesma decisão do quiz de 8 perguntas). `EtapasNav` (`etapas-nav.tsx`) substitui a `TopProgress` — uma barra de 1px que dizia quanto falta mas não onde você está nem como voltar. O desenho aprovado trazia DUAS afordâncias para os mesmos quatro destinos, pílulas na barra e uma régua numerada `01 MENSALIDADE · 02 QUIZ DE ROI · 03 RELATÓRIO · 04 EXPORTAR & FAQ`; **a pílula saiu no mesmo dia**, como o comentário do arquivo já previa — das duas, era a que só repetia o destino, enquanto a régua repete o destino **e** lê progresso (cada trilho mostra o quanto daquela etapa já foi vencido, não a posição do cursor). O `aria-label` da navegação e o `aria-current="step"` migraram junto: sem isso a remoção teria levado a landmark de navegação embora com os botões. **Nenhuma etapa é bloqueada**: voltar para conferir um número é o movimento mais comum de quem monta uma proposta, e a 03 já tem `ResultadoIncompleto` de rede — bloquear seria esconder a rede. **Etapa 01 (`etapa-mensalidade.tsx`) substitui a `IntroScreen`**: dois campos (vendedores + cadência) e a mensalidade aparece, porque é a pergunta que a pessoa veio fazer e respondê-la primeiro é o que compra as oito seguintes; ela grava em `times[0]`, então a pergunta 1 e a 8 já chegam semipreenchidas. **O painel do preço é AZUL DA MARCA** (`--pf-on-brand`/`-soft`/`-line` sobre `--pf-brand-deep`, decisão do decisor em 20/08/2026): reinstala com escopo estreito o "painel do preço" que o protótipo previa e a §13 tinha cortado por três razões — duas não o alcançam (o arquivo é exclusivo da jornada pública, então nenhum fallback precisa expressar inversão; e a etapa 01 não é bloco de resultado, é o painel de RESPOSTA de um passo, o mesmo papel da capa) e a terceira, o "nunca preencher blocos grandes de azul" da §1, é a exceção aberta **aqui e em lugar nenhum mais**. Duas coisas são contraste e não gosto: é o `--pf-brand-deep` e não o `--pf-brand` porque sobre o azul médio não existe tom secundário (`#b9c9e9` dá 3,33:1 e a primeira variante que passa já é branco, empatando a linha "Volume / Taxa efetiva" com a mensalidade acima); e é a etapa 01 e não a capa porque o verde `#0F9F2E` de "entra na conta" dá 2,38:1 sobre o azul — a capa tem o ROI em verde, a etapa 01 ainda não tem nada que some ao ROI. Dentro dele, um selo mono traz o benefício da oferta, com os meses saindo de `CASE_JANELA_MESES` (o mesmo número que o "case de sucesso" promete no fim da jornada — cravar "3" criaria um segundo lugar onde a janela mora). Os três bullets com ícone abaixo do CTA viraram uma linha só: duração, autosave e validade do link são ressalvas do botão, e em lista vertical pesavam mais que ele. **Etapa 02b (`etapa-avancada.tsx`) é a grade multi-equipes**: todos os campos de todos os times numa tabela, para comparar, mais o ajuste fino de deltas. A estrutura compartilhada (§4.11) migrou para lá, e com isso as oito perguntas ficaram **sem ramo de multi-time**: `BlocoEstrutura` trocou `passo: 1|3|4` por `secao: "gestores"|"salario"|"caminho"` e `PASSOS_DA_ESTRUTURA` deixou de existir. **O mapa de times continua à esquerda do quiz** (`times-sidebar.tsx`, que substitui `stepper-nav.tsx`): ele saiu por uma passagem e voltou por decisão do decisor, e a volta é fácil de justificar — é o único lugar de onde se enxerga o preenchimento de TODOS os times de uma vez, e no quiz ele não disputa espaço com um resultado. Duas coisas mudaram com a numeração: oito bolhas não cabem numa fita de 264px (8 × 44px passa de 350), então viraram grade **4 × 2** com o alvo de toque da §11 intacto; e a **pergunta 8 nunca fica verde**, porque `campos: []` faz `passoCompleto` devolver true por `every` — certo para o gating, mentira num tique que diria "respondida" numa pergunta que ninguém abriu. Remover um time de dentro do quiz mantém a pessoa no quiz, no time que sobrou: cair na etapa avançada seria trocar de assunto. **Etapa 04 (`etapa-exportar.tsx`)** reúne o que sai da tela, na ordem de uma reunião: `ResumoVerificavel` → case de sucesso → Fórmulas (PDF) → `EnviarBar` → o FAQ **inline** (o `FaqPainel` virou casca do novo `FaqLista`, e o modal do cabeçalho segue existindo para consulta durante o quiz). Saíram da jornada pública: `intro-screen`, `top-progress`, `seus-numeros-sidebar` e `CamposEstruturaSidebar` (`stepper-nav` foi substituído, não removido — ver `times-sidebar` abaixo).
- **O relatório abre por uma CAPA, e o cálculo detalhado continua abaixo dela** (`capa-resultado.tsx`, 20/08/2026). A pilha de dez blocos continua inteira — trajetória, as duas metades da soma, cascata, faixa de cenários, COI, preço —, mas ela deixou de ser a primeira coisa que se vê: quem abre o link decide primeiro se o número interessa e só então quer saber como ele foi construído. **A capa e o cálculo chegaram a ser duas abas e deixaram de ser no mesmo dia**: a segunda abria com um `HeroResultado` que repetia, noutro desenho, os quatro números que a capa acabara de dar, e escolher entre as duas era uma decisão que a pessoa não tinha por que tomar. Hoje é uma rolagem só, `onVerDetalhado` **rola** até `#ao-longo-de-12-meses` (seta para baixo, não para a direita — a seta lateral prometia tela nova) e `View` perdeu o campo `aba`. **O hero saiu da jornada pública e continua exportado**, porque `link-detail` (tela interna) o renderiza; a "checagem de realidade" que ele carregava no rodapé não se perdeu, já vivia também dentro do `PerformanceCard`. Quando o time ativo não fechou a conta, quem abre a etapa é `ResultadoIncompleto` e não a capa — uma capa inteira de travessões diz menos que a lista do que falta. A capa traz o painel invertido com quatro KPIs (ROI verde, retorno anual, payback, mensalidade), quatro cards de escopo, `AlocacaoValor`, a tabela de times e a régua diária. **Os quatro cards de escopo são todos slate e nenhum é acaso**: assentos e horas são escopo; gestor liberado e gestores não contratados são economia declarada que o motor mantém FORA da conta (`linhasNaoSomadas`) — pintá-los de verde diria que somam. Duas leituras são derivadas na apresentação, não no motor: prática/mês é `horasDoTime` (que já existia em `preco.ts`) e gestor liberado é `horasGestorDevolvidas` (`business-case.ts`). **`AlocacaoValor` é irmã da `CascataValor`, não substituta**: a cascata responde "como se chega ao total", a barra empilhada responde "de que o total é feito", e é a segunda pergunta que a capa endereça. Ela é **toda verde, em quatro degraus** — o desenho aprovado pintava a eficiência de preto e o ciclo de âmbar, mas as quatro parcelas somam ao ROI e a §1 reserva o âmbar para alerta.
- **O recálculo ao vivo saiu do resultado** (20/08/2026). Cenário, plano, assentos e prazo se escolhem na **pergunta 8** (`pergunta-proposta.tsx`) e o ajuste fino de deltas vive na etapa avançada; no relatório, `QuantoCusta` roda com `readOnly` e ganhou `cabecalho={false}` — quem nomeia o bloco agora é a `SecaoResultado` em volta, e o cabeçalho interno ainda dizia "a proposta que o CLIENTE montou", copy da tela interna, para o próprio cliente. O atalho "Ajustar proposta" leva de volta à pergunta 8, senão a leitura viraria beco sem saída. **O número parou de se mexer debaixo de quem está lendo** — que era o efeito de ter os sliders logo abaixo do próprio hero.
- **"Vamos criar um case de sucesso juntos" é o fecho da jornada** (`business-case.ts` + `case-sucesso.tsx`, no fim da etapa 04). É o único bloco que olha para FRENTE: tudo acima projeta doze meses, e aqui a proposta se compromete com noventa dias e quatro metas conferíveis. **As quatro metas NÃO são as quatro alavancas do ROI**, e o aviso âmbar diz isso em voz alta — ticket, conversão e ciclo levam mais de um trimestre para aparecer no pipe, e ancorar o case neles seria prometer prova que a janela não entrega. Rampa, tempo de gestor, adoção e satisfação se medem em 90 dias. **Módulo puro e derivado, no molde de `validacao-passo.ts`**: nenhuma premissa nova, cada meta é releitura noutra unidade de um número que `calcResultadoTime` já devolveu, e é por isso que ela pode ir a um contrato sem virar promessa nova. Dois cuidados pinados em teste: a rampa da meta aplica o `HAIRCUT` **aqui** (`deltas.rampaPct` é o delta CRU do cenário — quem aplica o 0,7 é `margemRampaAno`), senão o case prometeria uma rampa mais curta do que a que o ROI ao lado pagou; e `horasGestorDevolvidas` é o `min()` da eficiência **em horas** (ninguém devolve mais tempo do que hoje gasta treinando, nem mais do que o plano entrega), com `gestorLimitadoPeloPlano` dizendo à tela qual dos dois lados mordeu. `CASE_JANELA_MESES`, `CASE_ADOCAO_PCT` e `CASE_CSAT_MIN` são `[H]` comercial e moram em `constants.ts`. **O bloco não imprime**, de propósito: é um acordo a combinar no kickoff, e uma folha que já o trouxesse impresso o apresentaria como fechado.
- **O preenchimento explica cada campo na tela e cobra o que falta ali mesmo** (18/08/2026, a partir do mesmo handoff de UX; a camada de campo já tinha sido aproveitada antes de a arquitetura ser adotada). **(a) A descrição virou linha visível sob o rótulo.** `Field` sempre teve as duas formas — `help` (texto) e `hint` (ícone de info) — e o wizard passava `def.help` como `hint`, escondendo atrás de um alvo de 16px uma frase de uma linha e cobrando um tab stop por campo. `escala="leitura"` põe ajuda e erro em `text-sm`/`slate-600` (§8.12b). (A sidebar "Seus números" era a exceção — mantinha o `hint` em ícone, porque em 264px e 16 campos a pessoa ajusta um número que já entendeu. Ela saiu da jornada em 20/08; o `hint` segue disponível no `Field` para quem precisar do mesmo caso.) **(b) `alinhado` + `grid-rows-subgrid`**: com a ajuda visível, uma descrição de três linhas ao lado de uma de duas desalinhava os rótulos da mesma linha — o `items-end` antigo alinhava só os inputs. Agora rótulo, ajuda, input e erro dividem as mesmas faixas; a grade do passo declara `grid-rows-[auto_auto_auto_auto]`. **(c) O wizard passa a barrar o avanço** (antes "Continuar" sempre avançava e a pessoa só descobria a pendência no resultado). `validacao-passo.ts` é módulo puro que NÃO reimplementa obrigatoriedade — recorta `camposFaltando` pelos `PASSOS[].campos`, então mudar o gating em `calc.ts` arrasta a validação junto. **Duas mensagens por erro, de propósito**: a `curta` ("Preencha para continuar.") vai sob o campo, porque o rótulo está logo acima e repetir o nome empilhava cinco frases quase idênticas; a `mensagem` nomeia o campo e vai no `role="alert"` único do rodapé. O foco pousa no primeiro pendente. Saída digna: `ResultadoIncompleto` continua de rede. (**O stepper deixou de ser livre em 20/08/2026** — ver a régua de etapas abaixo. Voltar continua de graça; o que fechou foi o pulo para frente, que fazia do `ResultadoIncompleto` um destino em vez de uma rede.) **(d) O passo opcional diz que é opcional** (o do funil — a pergunta 7 desde 20/08, e é `PASSO_OPCIONAL` que a nomeia, nunca o número), com "Não tenho esses dados — pular esta etapa" que limpa os dois campos (a regra é dois ou nenhum). **(e) Enter avança** e o radiogroup do caminho ganhou roving tabindex + setas — tinha a semântica ARIA, não o comportamento, e cobrava quatro paradas de Tab para responder uma pergunta. **(f)** `aria-invalid`/`aria-describedby` ligam ajuda e erro ao input.
- **A etapa de resultado é uma pilha de blocos com `gap-2` (8px)** (`secao-resultado.tsx`), na ordem *O resultado → Ao longo de 12 meses → De onde vem o número → Quanto custa → Resumo*. Cada seção é um bloco branco próprio (`rounded-md border border-slate-200 p-6 sm:p-8`, via `moldura="painel"`); o hero usa `moldura="solta"` porque já é o próprio card. Blocos que quase se tocam: quem separa é a quebra de superfície, não a distância. Duas tentativas anteriores enquadram a decisão — (a) cards com `gap-4`/`gap-6`, que davam dez blocos de peso idêntico com até três `border-slate-200` encaixadas em `quanto-custa`; (b) **um** container único com `divide-y` e `py-10 sm:py-12` por faixa, que resolvia as molduras mas punha **~96px entre dois títulos vizinhos** (padding nas duas pontas), transformando respiro em buraco. Quem faz hierarquia agora é a ordem mais **um** bloco marcado: o hero (`tom="destaque"` em `resultado-time.tsx`) usa a mesma `border-slate-200` de todos, e se destaca pelo número um degrau acima (`--text-score-xl`) e por um **wash da primária na diagonal** sobre branco (`bg-white bg-linear-to-br from-primary/8 to-transparent to-60%`) — `bg-white` é a cor e o wash é a imagem, propriedades diferentes, sem depender da ordem das classes. Duas rotas foram testadas e descartadas nesse mesmo bloco: superfície verde inteira (`bg-surface-positive`), que puxava toda a paleta interna para `-ink` e fazia o bloco ler como peça de outro produto; contorno em gradiente azul→verde, cujas pontas brigavam com o fundo verde; e um **halo radial verde atrás do glifo** (`bg-radial from-trend-positive/14`), que chegou a substituir o wash no código e saiu em 18/08/2026 — halo sem deslocamento nem borrão é decoração, não profundidade, e destacava um caractere em vez do bloco. `bg-surface-positive` não tem mais consumidor na jornada: ficava só na celebração, e `celebracao-modal.tsx` saiu em 20/08/2026. Corolário: **os blocos de resultado não são donos da própria superfície** — `PaineisTrajetoria`, `EficienciaCard`, `PerformanceCard`, `CenarioSliders`, `QuantoCusta` e `ResultadoIncompleto` renderizam só `flex flex-col gap-*`, e é por isso que `link-detail` (interno) e a página do visitante compartilham os mesmos componentes sem um deles virar card aninhado. **Dentro** de um bloco o ritmo é outro: `SecaoResultado` mantém o corpo num filho só e aceita `ritmo="amplo"` (`gap-10`, 40px entre sub-tópicos) contra o `gap-6` padrão do título para o conteúdo. Só "De onde vem o número" usa — é o único bloco com mais de um assunto (o par Eficiência/Performance, a cascata, os parâmetros e a faixa de cenários), e com tudo em 24px os quatro valiam o mesmo que o cabeçalho: 1.800px sem cadência, nada dizendo onde um assunto acaba. Num bloco de assunto único 40px é buraco — não espalhe a prop. Hierarquia em três degraus, um papel cada (§2 das diretrizes): seção `text-lg slate-900` → bloco `text-sm font-semibold slate-700` → sub-rótulo e gatilho de `BlocoRecolhivel` `text-xs slate-500`. Não rebaixe a seção para o eyebrow de formulário: quando os três empatavam em `text-xs`, o título de primeiro nível lia igual a um disclosure dois níveis abaixo. O `ResumoVerificavel` traz a própria `SecaoResultado` (com o `id="resumo-verificavel"` que o CSS de impressão usa), então não o envolva de novo — o `id` nunca sobe para o container, ou a folha sai com hero e sliders.
- **A jornada da calculadora roda um degrau acima na escala tipográfica** (§8.12b das diretrizes): corpo em `text-base`, auxiliares em `text-sm`, cinzas parando em `slate-600`, rótulos de SVG em 12–13px. O leitor-alvo tem 45+ anos e abre o link uma vez para decidir — a escala de 14px/12px `slate-400` do app interno vira obstáculo. Não "corrigir" de volta achando que é inconsistência.
- **A jornada pública tem pele própria, `.pf-calc`** (§13 das diretrizes, 20/08/2026): canvas creme, superfícies quentes, numerais em mono e o campo editável em amarelo com texto azul — a legenda de cor da planilha que originou o produto ("fonte azul sobre fundo amarelo = seu input"). **A pele é creme + o AZUL da marca, não o verde do protótipo**: `#2a5d42` conviveria na mesma tela com o `#0F9F2E` que significa "entra na conta", e duas cores próximas com significados diferentes é o que a semântica da §1 existe para impedir. Escolher o azul fez a pele e os primitivos de `ui/` partilharem a mesma primária — é por isso que `Button variant="primary"`, `ring-primary/35` e `text-trend-positive` seguem intactos, e **não devem ser "tokenizados"**. **`@theme inline` inlina o hex na utilitária** (`.bg-primary` compila para `background-color: #2e63cd`, não `var()`), então sobrescrever `--color-primary` dentro da classe não faria nada: a pele se consome por valor arbitrário. Componente exclusivo da jornada usa a forma curta `bg-(--pf-canvas)`; componente que a tela interna também renderiza usa **sempre** fallback — `bg-[var(--pf-surface,#ffffff)]` —, e é isso que permite os mesmos blocos servirem às duas telas sem duplicação. Os tokens vivem em `.pf-calc` **e** em `body:has(.pf-calc)`, porque `Modal`, `SelectMenu`, `ActionMenu`, o Glossário e a barra de progresso fazem `createPortal` para o `document.body`: são irmãos do wrapper, não descendentes, e a variável não herdaria até lá. `design-tokens.test.ts` guarda a fronteira inteira — nenhum `--pf-*` no `:root` ou no `@theme`, nenhum token fantasma, fallback obrigatório nos compartilhados e em `ui/`, fallback sempre valor do design system (nunca da pele), zero `pf-` nas telas internas, e a lista de compartilhados é **derivada do fecho transitivo dos imports**, não escrita à mão: foi assim que `campo-numero` apareceu, três níveis abaixo por `link-detail → quanto-custa → campo-numero`, depois de a inspeção manual tê-lo classificado como exclusivo do visitante. **Três correções contra o material de origem:** `--pf-ink-faint` é `#6b6d65` e não o `#7c7f75` do protótipo (que dava 3,52:1 sobre o creme, abaixo do piso que a própria seção declara preservado); o eyebrow ficou sem caixa alta por três meses (o handoff trazia `uppercase` + `0.1em` justificando com uma leitura invertida da §2) — **e voltou a ter em 20/08/2026**, quando o decisor adotou o `DESIGN_SYSTEM.md` §3.2; a exceção é estreita e verificável, ver o item da escala tipográfica abaixo; e o alerta âmbar passa a se apoiar no fio e no ícone, porque sobre creme o `#FFFBEB` dá 1,12:1 e **nenhuma** areia mais escura resolve (`#f4ebdd` dá 1,02:1) — escurecer até amarelo de verdade colidiria com `--pf-input`, e dois amarelos com dois significados desfariam a coisa mais forte da pele.
- **A régua das quatro etapas é a única leitura de progresso, e ela trava o pulo para frente** (`etapas-nav.tsx`, 20/08/2026). Substituiu a `TopProgress` (fio de 1px que dizia quanto falta, não onde você está nem como voltar) e o stepper lateral. Cada etapa tem um trilho de 4px — **cheio e escuro quando vencida, fração em azul quando em curso** —, e a cor é o que separa os dois estados sem exigir que se compare largura. **`etapasLiberadas` é a regra, e a regra é uma só**: uma etapa abre quando TODAS as anteriores estão completas, mais a atual, que nunca tranca (estado restaurado pode pousar numa etapa cujas anteriores regrediram, e trancar o chão sob os pés seria pior que não ter trava). Disso sai de graça o que a decisão anterior protegia: **voltar continua livre**, porque etapa antiga não tem pendência atrás de si — o que fechou foi o pulo para frente, que levava a pessoa a um relatório sem números. A trava **depende das anteriores, nunca do preenchimento da própria etapa**: `exportar` só vale 1 depois do envio, e olhar para si mesma faria a última etapa não abrir nunca. **Dupla, no molde do botão "Fórmulas (PDF)"**: o controle fica visível e `aria-disabled` (não `disabled` de verdade — assim continua focável, e é pelo teclado que se descobre a condição), e `irParaEtapa` repete a checagem chamando **a mesma função**, não uma cópia. `etapas-nav.test.ts` pina os sete casos, incluindo os dois que a intuição erra: voltar sempre livre e a etapa atual nunca trancada.
- **A jornada roda a escala tipográfica do `docs/calculadora-design-system.md` §3.2** (decisão do decisor, 20/08/2026), que **substitui o §8.12b dentro de `.pf-calc`**. Oito níveis, uma classe CSS cada (`.pf-display`, `.pf-title`, `.pf-panel-title`, `.pf-label`, `.pf-lead`, `.pf-hint`, `.pf-num-hero`, `.pf-num-kpi`), todas com **regra base** (o degrau do §2, para `secao-resultado`/`quanto-custa`/`resultado-time`/`campo-numero`, que também rodam em `link-detail`) e **override dentro da pele**. São classes e não utilitárias porque cada nível carrega cinco propriedades que só significam juntas: espalhadas em `className`, a primeira cópia sai com quatro das cinco. **O pedido era mais hierarquia entre título, subtítulo e descrição, e o defeito não era o título ser pequeno — era o INTERVALO ser pequeno**: seção em 18px contra descrição em 16px, ambos sentence case, mesma família, cinza vizinho. O título de painel ENCOLHEU para 13px e ficou MENOR que a descrição, de propósito: caixa alta espaçada não é lida como frase, então ela encima o parágrafo em vez de disputar a leitura com ele — a hierarquia deixou de ser "qual é maior" e passou a ser "qual é outra coisa". **A §2 foi reaberta, e só nos dois níveis em caixa alta** (`.pf-panel-title` 13px `+0,12em`, `.pf-label` 12px `+0,10em`): a caixa mora em duas regras CSS, nenhum componente escreve `uppercase` no className (teste), e um terceiro `text-transform` no `globals.css` falha (teste de CONTAGEM — a versão frouxa "existe pelo menos um" deixaria a caixa alta se espalhar de novo). **A caixa é apresentação, nunca string**: `"MENSALIDADE ESTIMADA"` escrito na string era como a regra vinha sendo contornada em `etapa-mensalidade`, e custava a pronúncia no leitor de tela.
- **As famílias trocaram: Archivo + IBM Plex Mono** (§3.1 do DESIGN_SYSTEM), só dentro da pele — o app interno segue na Inter, que continua carregada com o 900. O mecanismo é uma **indireção**: `@theme inline` tem `--font-sans: var(--font-app)`, e `--font-app` vale `var(--font-inter)` no `:root` e `var(--font-archivo)` em `.pf-calc`. Apontar `--font-sans` direto para a Inter prenderia a utilitária `font-sans` a ela e a Archivo só alcançaria quem herdasse do body (o caso real é `placeholder:font-sans` em `campo-numero`). `--pf-mono` deixou de ser pilha de sistema porque o §3.2 pede peso 700 em dois níveis, e pilha de sistema não tem 700 previsível nem métricas iguais entre macOS e Windows — o mesmo KPI mudava de largura conforme a máquina.
- **"Tudo que é clicável e editável é alto contraste com o restante"** (decisão do decisor, 20/08/2026). Nasceram **dois fios**: `--pf-line` continua o **estrutural** (moldura de painel, divisória de conta — discreto de propósito, dez blocos não podem ter dez molduras gritando) e `--pf-line-strong` (`#b9b2a0`) é o de **controle** (card de opção não escolhido, botão secundário). Sem a segunda intensidade, controle e moldura dividiam a mesma borda e nada dizia onde se podia clicar. O campo editável é o topo da escala e ganhou a forma do §5.1: amarelo, borda de **1,5px**, sombra **interna** (`--pf-input-inset`) e valor em mono **700 de 16px** — é o inset que faz a célula parecer afundada em vez de pintada, que é a diferença entre "aqui você digita" e "aqui tem um destaque colorido".
- **A descrição do campo desceu para DEPOIS do input** (decisão do decisor, 20/08/2026), invertendo o "nunca a descrição depois do campo" da §8.10. A razão original era boa (ler antes de digitar erra menos), mas com o rótulo em caixa alta e o campo em amarelo, rótulo e input viraram um par visual forte, e uma frase de três linhas ENTRE eles separava o que a pessoa opera junto. `aria-describedby` continua ligando os dois, então o leitor de tela anuncia a ajuda no foco, antes de digitar, independentemente da ordem no DOM. **O `grid-rows-subgrid` do `Field` continua**, e ficou mais barato em vez de desnecessário: as quatro faixas agora são rótulo → input → ajuda → erro, e é a faixa do rótulo (altura do rótulo mais alto da linha) que garante o alinhamento superior pedido.
- **O verde do design system da calculadora (`#0E5E3F`) foi recusado** — decisão do decisor no mesmo dia: *"mantenha o azul atual, esqueça o verde desse DS"*. Mesma razão da recusa do `#2a5d42` do protótipo, que não envelheceu. **Do DESIGN_SYSTEM foram adotados a tipografia (§3), a forma do campo editável (§5.1) e a regra de contraste dos controles — não a paleta (§2.1).**
- **O hero não repete o que já está nele.** A frase abaixo do número traduz o múltiplo ("Cada R$ 1 investido devolve R$ 1,95 em margem no primeiro ano") em vez de recitar valor, investimento e payback, que estão nos KPIs ao lado. Os KPIs vêm na ORDEM DA CONTA (valor ÷ investimento → payback) e os dois primeiros na MESMA unidade, o ano: com valor anual contra mensalidade, quem confere dividindo achava doze vezes o ROI real. O mensal sobrevive como segunda linha do rótulo. O racional completo mora num `HintTooltip` ao lado de "retorno projetado no primeiro ano" — é a primeira pergunta de quem banca a conta, e ela não pode custar uma rolagem. O selo "Estimativa" e a ressalva "projeção, não medição" saíram do hero: a tela abria pedindo desculpa pelo número, e a ressalva já vive no `Disclaimer` do resumo (invariante 10 preservado).
- **O Resumo não repete as travas.** A lista "Verificável nesta tela" foi removida: as mesmas travas estão em "Perguntas comuns" com o argumento inteiro, e duplicadas engordavam o fecho da página. Sobraram a síntese em prosa, os três KPIs, a régua diária, a tabela da Oferta e o disclaimer — o que o papel precisa.
- **Dois tons de hero** (`TOM` em `resultado-time.tsx`): o destaque é o wash diagonal sobre branco descrito acima (a antiga versão em `bg-surface-positive` cheio foi substituída — `-ink` só faz sentido sobre o verde, que saiu daqui). `SeloEvidencia` tem `tom="destaque"` com paleta própria (substituição, não acréscimo: `cn` é clsx puro). O `tom="claro"` sobrevive num lugar só — o hero consolidado de `link-detail`, que vive fora do container, sobre o fundo da página.
- **Quatro gráficos SVG próprios** (`graficos-resultado.tsx`, zero dependências, convenções de `trajetoria-panel.tsx`): `CascataValor` (eficiência + as 4 alavancas → valor do ano; **absorveu a antiga `EquacaoValor`**, que dizia a mesma soma em texto e foi removida do código em 18/08/2026 — ficou exportada e órfã por uma passagem; a prosa sobrevive como legenda acessível abaixo do desenho), `ComparacaoCenarios` (3 colunas contra a linha tracejada do investimento; o rótulo da linha vive na legenda HTML, porque dentro do SVG cruzava a barra mais alta), `EscadaPrecoGrafico` (faixas proporcionais em tons de slate, dentro do `BlocoRecolhivel` do preço — **tudo slate, custo não é verde**) e `MedidorChecagem` (bullet gauge com o tique dos 25%, no rodapé do hero e no card interno; slate/âmbar, nunca verde — a checagem é freio, não parcela). Nenhum é dono da própria superfície e nenhum vive sob `#resumo-verificavel`, então nenhum imprime: a folha continua sendo só o Resumo.
- **"Perguntas comuns"** (`faq.ts` puro + `faq-cfo.tsx`): 12 dúvidas interpoladas com os números DO LINK, agrupadas em três frentes (`GRUPOS`: cálculo / custo e risco / programa) — numa lista plana de doze, achar a própria dúvida custa ler as outras onze. Vive **só no modal** aberto pelo botão "Perguntas comuns" no header. Como seção na página, as doze empilhadas viravam uma faixa sem ritmo: texto curto à esquerda, metade da largura vazia à direita, e nenhum bloco puxando o olho para o próximo.
  - **As perguntas são dúvidas de quem lê, não objeções encenadas.** "E se contratarmos e não funcionar? Ficamos presos" é fala de um terceiro numa reunião imaginária, e lê como roteiro de vendas; "Qual é a exposição se não funcionar?" é a dúvida real. Idem nos grupos: "Se duvidarem do número" punha o leitor na defensiva de alguém que não está na sala, e virou "Sobre o cálculo". Um teste garante que toda pergunta termine em interrogação. O campo `subtexto` ("a pergunta por trás da pergunta") foi removido pelo mesmo motivo.
  - **O rótulo não nomeia a técnica nem o cargo de quem lê.** "Objeções", "quebra de objeção" ou "o que um CFO perguntaria" entregam que a peça é roteiro comercial — o que queima a credibilidade que a tela passa cinco passos construindo — e ainda presumem o cargo do leitor: quem preenche quase nunca é quem assina, é quem vai **defender** o número na mesa. A peça é munição para essa defesa, não interrogatório contra quem lê. Uma versão anterior chamava-se "Perguntas que um CFO faria" e foi reprovada por isso.
  - **O acesso pelo header é MODAL, não gaveta como o Glossário** (`Modal` de `ui/modal.tsx`, `max-w-2xl`, rolagem interna em `max-h-[65dvh]`). Os dois são consulta, de naturezas opostas: o glossário é olhada de um segundo e não pode tirar o formulário da tela; aqui a leitura é longa e densa, e quem lê precisa parar. Overlay e trava de scroll fazem esse trabalho — a gaveta, com a página viva ao lado, convidava a passar o olho e voltar a mexer nos campos.
  - A copy é escrita a partir do motor e **nunca transcrita** do `Perfecting-CFO-QA-Guide.docx`, que descreve uma oferta que não existe aqui (desconto por prazo, contrato de 18 meses, plano "Enterprise", haircut em 4 das 5 alavancas, "801 fórmulas"): citar aquilo entregaria um número que a tela ao lado desmente. `faq.test.ts` casa cada número citado com os goldens, barra as promessas falsas e garante que nenhum grupo fique vazio. Sem resultado, as respostas existem sem número — nunca travessão de campo vazio no meio da prosa.
- **Passagem de layout de 18/08/2026** (`/impeccable layout` sobre a etapa de resultado). Seis achados, todos de arranjo — nenhum número do motor mudou: **(a)** o `=` do hero era item solto num `flex-wrap` e, em coluna estreita, a conta quebrava depois dele: a primeira linha terminava num operador pendurado e o múltiplo descia sozinho. Operador e resultado agora são um grupo — quebram juntos ou não quebram. **(b)** O halo verde saiu (acima). **(c)** Em `LinhaCompacta` o valor leva `ml-auto`: com rótulo longo o `justify-between` sozinho jogava o valor para a **esquerda** na linha seguinte, e a coluna de valores deixava de existir no meio da lista. **(d)** `MedidorChecagem` ancorava o rótulo do valor na origem do trilho, nomeando a ponta esquerda de uma barra que acaba à direita; agora o rótulo mora onde a barra termina (âncora vira `start` perto da origem, para não sair do viewBox) e a régua do limite é traço inteiro — tracejada em 20px de altura lia como sujeira. **(e)** Em `quanto-custa` a caixa tingida em volta da proposta saiu: painel branco › `bg-slate-50/60` › botão bordado eram três superfícies para dizer que uns controles andam juntos, e a proximidade já dizia. Multi-time separa por fio. O total do contrato ganhou `border-t` (fio = fecha conta, abaixo) e os pares "Assentos / Prática contratada" alinham pelo topo — com `items-end` o valor derivado assentava na altura do **texto de ajuda**, não do campo. **(f)** Auxiliares em `text-xs` `slate-400`/`slate-500` subiram para `text-sm` `slate-600` (§8.12b — `text-xs` fica para selo e chip): era o texto que explica cada slider e cada plano, no menor e mais claro ajuste da tela. O que **não** mudou, e não é esquecimento: a pilha continua em `gap-2`, cada seção continua com moldura branca, e a escala segue 8/4 — o teto de 48px de quebra de seção já foi testado e rejeitado (o buraco de 96px acima).
- **Passagem de polish de 18/08/2026** (`/impeccable polish`, logo após a de layout). Quatro defeitos, dois deles de acessibilidade: **(a)** `CampoNumero` era o único controle do produto sem foco visível — o `input` tem `outline-none` e a única resposta ao foco era a borda de 1px virando `#2E63CD`/40, indistinguível de `slate-200` a um palmo da tela. Ganhou o `focus-within:ring-2 ring-primary/35` que todo o resto usa; são 16 campos só na sidebar. **(b)** Contraste: placeholder em `slate-300` dava 1,6:1 e os afixos de unidade (R$, %, h) em `slate-400` davam 3,0:1 — ambos abaixo do piso de 4,5:1. Placeholder foi para `slate-500` e não para `slate-600` como o resto da jornada de propósito: no degrau do corpo ele passa a ser lido como valor preenchido, que é defeito pior que o contraste. **(c)** A lista rolável da sidebar cortava o rótulo seguinte no meio dos glifos, e o corte duro lia como bug de overflow; a máscara no rodapé vira afordância e não come conteúdo (hoje `rolagem-esvanecida-y`, abaixo). **(d)** `LinhaCompacta` virou grade de duas trilhas: em `flex-wrap` + `justify-between` o rótulo comprido empurrava o valor para uma segunda linha e metade das linhas da lista virava duas. Na grade quem quebra é o rótulo, dentro da própria trilha.
- **Barra de rolagem nenhuma na etapa de resultado; quem avisa é o esvanecer** (`rolagem-esvanecida-y` / `-x` e `rolagem-limpa`, em `globals.css`). A barra nativa cruzando o rodapé de um gráfico é ruído de sistema numa tela que a pessoa abre uma vez para decidir. Esconder barra sem devolver afordância seria regressão, então as duas coisas moram na MESMA classe: `scrollbar-width: none` mais um degradê de 24px na ponta que ainda esconde conteúdo — embaixo no eixo vertical (sidebar), à direita no horizontal (`CascataValor`, `ComparacaoCenarios`, a estante de 12 sliders da trajetória). O degradê é atado à posição da rolagem (`animation-timeline: scroll(self …)` animando só o comprimento `--rolagem-fim`, registrado por `@property`): recua nos últimos 32px do curso, para a última linha chegar inteira, e **some quando não há o que rolar** — timeline sem overflow é timeline inativa, a animação não se aplica e o estado base é máscara opaca. É isso que permite marcar um SVG que só transborda no mobile sem apagar a borda dele no desktop, e é isso que conserta de quebra a máscara fixa anterior da sidebar, que escurecia o último campo no mobile (onde a coluna não tem `max-h` e não rola). Exceção única: a fita de abas da trajetória usa `rolagem-limpa` (só esconde a barra) — é uma pílula com borda, e o degradê dissolveria a borda junto. A barra do documento e a do modal de "Perguntas comuns" continuam nativas: numa leitura longa a posição na barra é informação.
- **Um selo por grupo, não por linha.** Em `EficienciaCard` as duas parcelas que não entram na conta carregavam cada uma a pílula `nao_somado` dentro do rótulo. Dois selos dizendo a mesma coisa, cada um empurrando o rótulo para uma segunda linha e a si próprio para uma terceira — decoração que existia só para desempatar duas linhas de quatro. Agora são dois grupos: as somadas, um fio, e `Não somado ao ROI` como rótulo do par. A prop `selo` de `LinhaCompacta` saiu junto (não tinha outro chamador), e `EquacaoValor` — exportada e órfã desde que a `CascataValor` a absorveu — foi removida.
- **Fio = fecha conta.** Em `Linha` o `border-t` existe só na linha `destaque` (subtotal), com `first:border-t-0`. Um fio entre cada duas linhas transformava sete parcelas em sete réguas e a lista lia como planilha; o espaço (`gap-5`) separa as parcelas.
- **Entre o quiz e o relatório existe uma TELA, não um modal** (`celebracao.ts` + `processando-resultado.tsx`, 20/08/2026). Era um modal de comemoração: o número aparecia atrás dele e a pessoa fechava uma janela para chegar ao próprio resultado. `ProcessandoResultado` não pede clique nenhum — mostra quatro passos que se completam em **3 s** e entrega o relatório sozinha. Os quatro passos não são teatro (são o que `computarModelo` de fato faz, na ordem); o que é deliberado é o RITMO, porque o cálculo é instantâneo e a espera existe para separar "eu preenchi" de "aqui está a resposta", que é a fronteira que o modal marcava — por isso a tela descreve o trabalho e **nunca promete duração**. Não mostra número nenhum, então não é tela de resultado e não carrega o `Disclaimer` (invariante 10 preservado). Os tiques concluídos são **azuis**: verde ali emprestaria a uma etapa de carregamento o significado de "entra na conta". `irParaRelatorio()` é a **única** porta da etapa 03 e é onde a decisão mora — atravessa o processamento quem acabou de fechar a conta de um time nesta sessão, entra direto quem já sabia o número. O time é marcado como visto na ENTRADA da tela, não na saída: quem sai no meio não é interceptado de novo. **O corte de sessão é o mesmo de antes** (`celebracao.ts`, cujo nome é herança do modal): o modal abria uma vez por time que ficou completo **nesta sessão**. A semente é `timesJaCompletos(prog)` num `useRef` do primeiro render — é ela que impede o modal de receber quem volta com o link já preenchido (que cai direto em `{modo:"resultado"}`). A lista de vistos só cresce, então incompletar e recompletar não reabre. Nada é persistido: o link é o estado e "já comemorou" é preferência de apresentação, não dado do cliente. Nenhum evento novo, nenhuma coluna nova — o envio segue com o feedback discreto do `EnviarBar`.
- **O Resumo é o único bloco que imprime**, e por isso repete em prosa o que a página já mostrou: é lido fora da tela, sem o hero acima. Contém síntese, os 3 KPIs, a régua diária contra o gestor, as travas, a tabela da oferta e **o `Disclaimer` dentro dele** — só o que está sob `#resumo-verificavel` fica visível na impressão, e a folha sem disclaimer violaria o invariante 10. `calculadora-app` só renderiza o `Disclaimer` avulso quando o consolidado está incompleto (aí o Resumo não existe). Copy da oferta compartilhada com "Quanto custa" vive em `NIVEL_COPY`/`PRAZO_COPY` (`constants.ts`), nunca duplicada no componente.
- **Verde = entra na conta.** `text-trend-positive` (nunca o hex literal; em SVG, a constante `VERDE` de `trajetoria-panel.tsx`) marca só as parcelas que somam ao ROI: as quatro alavancas, a economia declarada, os dois subtotais, as parcelas da `CascataValor` e o valor/ano. Linha com selo "não somado ao ROI" fica slate — verde ali contradiria o selo ao lado. Preço, investimento e payback também são slate: custo não é "ruim", é a outra metade da fração, e payback é tempo. A prop é `tom="positivo"` no componente `Linha`. A trajetória vem logo depois do hero: o convite a desenhar a curva precisa chegar antes do cansaço. Os três painéis de trajetória vivem num card só, em abas locais (não use `ui/tabs.tsx`: ele sincroniza com `?tab=` pelo router, e a página pública é de estado local). Blocos secundários — extrato do preço, detalhe do resumo imprimível — usam `BlocoRecolhivel`, que colapsa por `grid-template-rows` e **mantém o conteúdo montado**: `display:none` não sai na impressão. Cuidado com altura: a checagem de realidade é rodapé do hero (não card próprio), e o par Eficiência/Performance usa **container query** (`@3xl` na coluna), não breakpoint de viewport — nunca `xl:`, que empilhava em qualquer janela abaixo de 1280px. A container query sobreviveu à saída da sidebar em 20/08 e ficou MAIS certa por isso: a coluna agora é a largura toda, então o par abre em duas colunas em telas onde antes não abria, sem que nada no componente tenha mudado. A sidebar "Dados preenchidos" (recolhível, com rail vertical no desktop e sanfona no mobile) saiu junto com `seus-numeros-sidebar.tsx` — quem edita um número agora é o próprio quiz, alcançável pela etapa 02 no cabeçalho.
- **Trajetória: três abas, edição por sliders** (`trajetoria-panel.tsx`). *Margem acumulada* (padrão, §4.12: gap no mês 12 = G) é leitura pura; *Margem mensal* é onde a curva desenhada aparece e é editada — no acumulado a soma achata a oscilação; *Em quanto tempo se paga* é o painel B. Editar abre uma estante de **12 sliders verticais** (um por mês, `.slider-trajetoria` em `globals.css`), cada um clampado por `pisoPonto`/`tetoPonto` via `limitesMensais` — nenhum limite novo, os tetos do §5 seguem donos únicos. O rascunho vive **só no componente**: a soma anda livre ("somam 149% do total projetado", `somaRelativa`) e nada é persistido até "Concluir edição", que chama `reReconciliar` — o reajuste proporcional que restaura `Σg = G` preservando a forma. Não persistir o rascunho é o que impede o autosave de gravar soma inválida e a página de re-renderizar a cada frame.
- **Erratas abertas no V5** (§1.6 da auditoria de 15/08/2026, a numerar E-12…E-16): o documento usa `PCT_EVENTO_SUBSTITUIVEL` sem definir o valor; define margem "por faixas" sem enumerar os valores centrais; lista a folha de resumo PCO (§10) como especificada, sem que exista no código; o §4.12 descreve só dois painéis, sem o mensal; e descreve a edição como arrasto de checkpoints com reconciliação **a cada movimento**, quando a implementação reconcilia **no commit da edição**. Nada disso é divergência de cálculo — `Σg = G` vale em todo estado persistido e a trajetória continua fora de `calcResultadoTime` (invariante 4). É o documento que precisa alcançar o código, como o próprio §4.12 já fez ao registrar "reconciliação iterativa em vez de aditiva".

## Decisões registradas

- `@heroico/utilities` (do prompt original) não existe no npm — usamos `clsx` + `cn()` em `src/lib/utils.ts`.
- Kanban mostra **todos** os clientes (inclusive `encerrado`), com StatusChip compacto quando ≠ ativo.
- Matriz RLS exata do prompt: role `cliente` não tem insert em `events` → upload de arquivo por cliente não gera evento.
- Upload vai do browser direto ao Storage (policies protegem); a server action só registra metadados e revalida o limite de 20 MB.
- Sem evento de exclusão de cliente (events têm FK cascade). Tendência de "Atividades atrasadas" não é reconstituível pelo log → sempre "—".
- Seed não referencia `auth.users` (colunas de ator são NULL); o primeiro cadastro real vira profile `interno` via trigger.
- Marketing usa **service role** (`src/lib/supabase/service.ts`, importado só por `src/lib/api/marketing-queries.ts`). Policies `to anon` seriam piores: a anon key é `NEXT_PUBLIC_` e exporia as tabelas direto no PostgREST, sem token, CORS nem rate limit. As tabelas mantêm RLS `is_interno()` como backstop da UI.
- Post "agendado" **não é status no banco**: é derivado de `publicado` + `published_at` futuro (`postState`). Um predicado só na API e nenhum cron. `arquivado` é status real — despublica sem perder a data.
- Perguntas do funil ficam em **jsonb** (como `activities.subatividades`); o que é publicado vira linha imutável em `marketing_funnel_versions`, e o lead aponta para a versão via FK. Editar o rascunho não reescreve o significado de leads antigos.
- `publicQuestions()` remove `score`, `weight` e `maps_to` antes de entregar ao site — vazar pesos ensinaria o visitante a se auto-qualificar.
- Markdown tem **parser próprio** (`marketing-markdown.ts`) que devolve AST tipada, não HTML: zero deps, zero `dangerouslySetInnerHTML`, testável em `environment: "node"`.
- Imagem no corpo do post é **markdown, não coluna**: o editor sobe o arquivo para `marketing-media/posts/{id}/corpo/` e insere `![alt](url)` absoluta (botão, colar ou arrastar). Sem server action — a referência vive no texto e só persiste quando o post salva; por isso `deletePost` limpa a **pasta inteira** do post, não só a capa. `insertImage` força linha em branco em volta: o parser só vê bloco de imagem quando ela está sozinha na linha.
- **`#` é do título do post, não do corpo**: o site já renderiza `post.title` como o `<h1>` da página, então a barra do editor insere `##`/`###` e `hasTitleHeading` acusa `#` remanescente (com ação de rebaixar). É aviso, nunca bloqueio — o gate de publicar continua sendo só o `validatePostInput`.
- Anti-spam no envio: honeypot `hp` e `elapsed_ms < 1500` respondem `201` **sem gravar** (bot não recebe sinal). Rate limit em memória é por instância; o throttle que atravessa lambdas é a contagem por `ip_hash` no banco. LGPD: guardamos só o HMAC do IP.
- Excluir funil com leads é **recusado** (arquive); excluir cliente/post limpa o Storage à mão, porque o cascade não alcança o bucket.
- CSV de leads usa `;` + BOM: é o único par que o Excel pt-BR abre em colunas e sem mojibake. Uma coluna por pergunta, deduplicada por id — exportar funis diferentes juntos gera matriz esparsa, mas nenhuma resposta troca de coluna.
